import { LegalAgeDirective } from './legal-age.directive';

describe('LegalAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new LegalAgeDirective();
    expect(directive).toBeTruthy();
  });
});
