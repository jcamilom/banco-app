import { MaxTodayDirective } from './max-today.directive';

describe('MaxTodayDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxTodayDirective();
    expect(directive).toBeTruthy();
  });
});
